# Runway Dev API — server-side only, never expose to the browser.
# Get a key from the Developer Portal: https://dev.runwayml.com (API Keys).
# Requires credits on the org before generation calls will succeed.
RUNWAYML_API_SECRET=
